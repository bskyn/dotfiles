# Global Instructions

- Default to using FFF for repository navigation and search when available.
- Use `mcp__fff__find_files` for finding files by name.
- Use `mcp__fff__grep` or `mcp__fff__multi_grep` for searching file contents.
- Fall back to `rg` or other shell tools only when FFF is unavailable or unsuitable for the task.
